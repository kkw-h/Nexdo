# Nexdo App

Flutter 前端位于 `app/` 目录。

## 常用命令

```bash
flutter pub get
flutter run -d macos
flutter test
flutter analyze
```

## AltStore 安装

如果希望通过 AltStore/AltServer 在 iPhone 或 iPad 上侧载 Nexdo，可以参考 [`../docs/ALTSTORE.md`](../docs/ALTSTORE.md)。该文档包含打包 `.ipa`、导入 AltStore 以及配置 AltStore Source 的完整步骤。
